{
  "name": "MMM-TelegramBot",
  "version": "1.0.0",
  "dependencies": {
    "@google-cloud/speech": "^0.10.3",
    "google-assistant": "^0.1.12",
    "node-aplay": "^1.0.3",
    "node-record-lpcm16": "^0.3.0",
    "snowboy": "^1.2.0",
    "speaker": "^0.4.0"
  },
  "scripts": {
    "preinstall": "sudo apt-get update && sudo apt-get upgrade && sudo apt-get install libmagic-dev libatlas-base-dev espeak libasound2-dev sox libsox-fmt-all libttspico-utils",
    "postinstall": "cd MMM-Assistant && npm install --save-dev electron-rebuild && ./node_modules/.bin/electron-rebuild --pre-gyp-fix"
  },
  "devDependencies": {
    "electron-rebuild": "^1.6.0",
    "yarg": "^1.0.8"
  }
}
